import { useFormState } from 'aetherspace/forms/useFormState'
import { DeepPartial } from 'aetherspace/types/typeHelpers'
import { z, aetherSchema } from 'aetherspace/schemas'

/* --- Schema & Types -------------------------------------------------------------------------- */

export const {{SchemaName}} = aetherSchema('{{SchemaName}}', {
  test: z.string().default('hello world'),
})

export type T{{SchemaName}} = DeepPartial<typeof {{SchemaName}}>

{{formHookDivider}}

const {{formHookName}} = (initialValues: T{{SchemaName}}) => {
  const formState = useFormState({
    stateSchema: {{SchemaName}},
    initialValues,
    applyDefaults: true,
  })
  return formState
}

/* --- Exports --------------------------------------------------------------------------------- */

export default {{formHookName}}
