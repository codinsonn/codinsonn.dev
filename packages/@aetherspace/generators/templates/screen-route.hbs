import React from 'react'
// Navigation
import { AetherPage } from 'aetherspace/navigation'
// Screens
import * as {{screenName}} from '{{screenImportPath}}'

/* --- Config ---------------------------------------------------------------------------------- */

const ScreenComponent = {{screenName}}.{{ScreenName}}
const screenConfig = {{screenName}}.screenConfig

{{{routePathDivider}}}

const PageScreen = (props: {{screenName}}.T{{ScreenName}}Props) => (
  <AetherPage {...props} screen={ScreenComponent} screenConfig={screenConfig} />
)

/* --- Exports --------------------------------------------------------------------------------- */

export const dynamic = screenConfig.dynamic

export default PageScreen
