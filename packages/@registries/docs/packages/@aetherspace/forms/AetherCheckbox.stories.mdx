import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as AetherCheckbox, getDocumentationProps as getAetherCheckboxProps } from '../../../../../../packages/@aetherspace/forms/AetherCheckbox/AetherCheckbox.tsx'
// Mocks
import StoryContextProviders from '../../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="packages / @aetherspace / forms / AetherCheckbox" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AetherCheckbox

export const AetherCheckboxDocs = createDocs(AetherCheckbox)
export const AetherCheckboxConfig = aetherStoryDocs({ AetherCheckbox }, getAetherCheckboxProps)

`/packages/@aetherspace/forms/AetherCheckbox/AetherCheckbox.tsx`
```typescript
import AetherCheckbox from 'aetherspace/forms/AetherCheckbox'
```


<Canvas>
    <Story
        name="AetherCheckbox"
        args={AetherCheckboxConfig.args}
        argTypes={AetherCheckboxConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#FFFFFF' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherCheckboxDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherCheckboxConfig.args) && <ArgsTable story="AetherCheckbox" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

