import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { BioLink, getDocumentationProps as getBioLinkProps } from '../../../../../features/app-core/components/BioLink.tsx'
// Mocks
import StoryContextProviders from '../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="features / app-core / components" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## BioLink

export const BioLinkDocs = createDocs(BioLink)
export const BioLinkConfig = aetherStoryDocs({ BioLink }, getBioLinkProps)

```typescript
import { BioLink } from 'features/app-core/components/BioLink'
```


<Canvas>
    <Story
        name="BioLink"
        args={BioLinkConfig.args}
        argTypes={BioLinkConfig.argTypes}
    >
      {BioLinkDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(BioLinkConfig.args) && <ArgsTable story="BioLink" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

