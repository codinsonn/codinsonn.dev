import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { ResumeScreen, getDocumentationProps as getResumeScreenProps } from '../../../../../features/cv-page/screens/ResumeScreen.tsx'
// Mocks
import StoryContextProviders from '../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="features / cv-page / screens" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## ResumeScreen

export const ResumeScreenDocs = createDocs(ResumeScreen)
export const ResumeScreenConfig = aetherStoryDocs({ ResumeScreen }, getResumeScreenProps)

`/features/cv-page/screens/ResumeScreen.tsx`
```typescript
import { ResumeScreen } from 'cv-page/screens/ResumeScreen'
```


<Canvas>
    <Story
        name="ResumeScreen"
        args={ResumeScreenConfig.args}
        argTypes={ResumeScreenConfig.argTypes}
    >
      {ResumeScreenDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(ResumeScreenConfig.args) && <ArgsTable story="ResumeScreen" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

